query SchedulingByUser($userId: String!) {
    schedulesByUser(user_id: $userId) {
        id
        date
        hour
        id_reservation
        state
        medicalprofessional {
            id
            name
            lastName
        }
    }
}